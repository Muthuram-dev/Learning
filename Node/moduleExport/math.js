const add = (x,y) =>  x + y;

const pi = 3.14;

const square = (x) => x * x;

const math ={
    add : add,
    square : square,
    pi : pi
}

module.exports = math;

// module.exports.add = add;
// module.exports.square = square;
// module.exports.pi = pi;
